import { useTranslation } from 'react-i18next'
import { motion } from 'framer-motion'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import { Badge } from '@/components/ui/badge'
import { Separator } from '@/components/ui/separator'
import { Code, Terminal, AlertCircle, CheckCircle } from 'lucide-react'

export default function ApiDocs() {
  const { t } = useTranslation()

  const endpoints = [
    {
      key: 'upload',
      method: 'POST',
      color: 'bg-green-500',
      requiresAuth: false
    },
    {
      key: 'status',
      method: 'GET',
      color: 'bg-blue-500',
      requiresAuth: false
    },
    {
      key: 'info',
      method: 'GET',
      color: 'bg-purple-500',
      requiresAuth: false
    },
    {
      key: 'download',
      method: 'GET',
      color: 'bg-orange-500',
      requiresAuth: false
    }
  ]

  return (
    <div className="space-y-6 max-w-4xl mx-auto">
      <motion.div
        initial={{ opacity: 0, y: 20 }}
        animate={{ opacity: 1, y: 0 }}
        className="text-center space-y-2"
      >
        <h1 className="text-2xl sm:text-3xl font-bold tracking-tight">
          {t('apiDocs.title')}
        </h1>
        <p className="text-sm text-muted-foreground">
          {t('apiDocs.description')}
        </p>
      </motion.div>

      <Card>
        <CardHeader>
          <CardTitle className="text-lg flex items-center space-x-2">
            <Terminal className="h-5 w-5" />
            <span>{t('apiDocs.baseUrl')}</span>
          </CardTitle>
          <CardDescription>
            <code className="px-2 py-1 bg-muted rounded text-sm">
              https://kabox.my.id/api
            </code>
          </CardDescription>
        </CardHeader>
      </Card>

      <div className="grid gap-4">
        {endpoints.map((endpoint, index) => (
          <motion.div
            key={endpoint.key}
            initial={{ opacity: 0, x: -20 }}
            animate={{ opacity: 1, x: 0 }}
            transition={{ delay: index * 0.1 }}
          >
            <Card>
              <CardHeader className="pb-3">
                <div className="flex items-center justify-between">
                  <div className="flex items-center space-x-3">
                    <Badge className={`${endpoint.color} text-white`}>
                      {endpoint.method}
                    </Badge>
                    <CardTitle className="text-base">
                      {t(`apiDocs.endpoints.${endpoint.key}.title`)}
                    </CardTitle>
                  </div>
                </div>
                <CardDescription>
                  {t(`apiDocs.endpoints.${endpoint.key}.description`)}
                </CardDescription>
              </CardHeader>
              <CardContent className="space-y-4">
                <div className="space-y-2">
                  <p className="text-xs font-medium text-muted-foreground uppercase tracking-wider">
                    Path
                  </p>
                  <code className="block p-3 bg-muted rounded-lg text-sm font-mono">
                    {t(`apiDocs.endpoints.${endpoint.key}.path`)}
                  </code>
                </div>

                <div className="space-y-2">
                  <p className="text-xs font-medium text-muted-foreground uppercase tracking-wider">
                    Example Request
                  </p>
                  <div className="relative">
                    <code className="block p-3 bg-muted rounded-lg text-sm font-mono overflow-x-auto">
                      {t(`apiDocs.endpoints.${endpoint.key}.example`)}
                    </code>
                  </div>
                </div>

                {'response' in t(`apiDocs.endpoints.${endpoint.key}`, { returnObjects: true }) && (
                  <div className="space-y-2">
                    <p className="text-xs font-medium text-muted-foreground uppercase tracking-wider">
                      Response
                    </p>
                    <pre className="p-3 bg-muted rounded-lg text-sm overflow-x-auto">
                      {JSON.stringify(t(`apiDocs.endpoints.${endpoint.key}.response`, { returnObjects: true }), null, 2)}
                    </pre>
                  </div>
                )}
              </CardContent>
            </Card>
          </motion.div>
        ))}
      </div>

      <Card className="border-yellow-500/50 bg-yellow-50/50 dark:bg-yellow-900/10">
        <CardHeader>
          <CardTitle className="text-lg flex items-center space-x-2">
            <AlertCircle className="h-5 w-5 text-yellow-600" />
            <span>{t('apiDocs.limits.title')}</span>
          </CardTitle>
        </CardHeader>
        <CardContent className="space-y-2">
          <div className="flex items-start space-x-2 text-sm">
            <CheckCircle className="h-4 w-4 text-green-500 mt-0.5" />
            <span>{t('apiDocs.limits.rateLimit')}</span>
          </div>
          <div className="flex items-start space-x-2 text-sm">
            <CheckCircle className="h-4 w-4 text-green-500 mt-0.5" />
            <span>{t('apiDocs.limits.maxSize')}</span>
          </div>
          <div className="flex items-start space-x-2 text-sm">
            <CheckCircle className="h-4 w-4 text-green-500 mt-0.5" />
            <span>{t('apiDocs.limits.maxFiles')}</span>
          </div>
        </CardContent>
      </Card>
    </div>
  )
}
